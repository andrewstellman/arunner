"""arunner engine — the stdlib tick state-machine, ticker, heartbeat helper,
jobs expander, in-context core, and demo worker (shipped inside the package so
the installed console script works). FR-33/34 packaging."""
